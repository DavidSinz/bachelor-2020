[DATABASE]
dbcreatetable = id INT PRIMARY KEY, name VARCHAR(255) NOT NULL, path VARCHAR(4096), code VARCHAR(255) NOT NULL, link_id INT NOT NULL, set_id INT NOT NULL, file_type VARCHAR(8), size INT, type VARCHAR(8) NOT NULL, insert_date DATETIME NOT NULL, trash INT NOT NULL, data_name VARCHAR(255), screenshot VARCHAR(255)
dbcolumns = id, name, path, code, link_id, set_id, file_type, size, type, insert_date, trash, data_name, screenshot